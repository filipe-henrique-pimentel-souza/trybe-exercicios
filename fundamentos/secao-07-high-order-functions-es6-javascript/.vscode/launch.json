{
    "version": "0.2.0",
    "configurations": [
    
        {
            "type": "node-terminal",
            "name": "Terminal de Depuração de JavaScript",
            "request": "launch",
            "cwd": "${workspaceFolder}"
        }
    ]
}